from setuptools import setup, find_packages

setup(
    name="shared_models",
    version="0.1.0",
    packages=find_packages(),
    description="Modelos de datos Pydantic compartidos para el SGA ISO 14001:2026."
)